/*
 * Minimal stub — Task 7 replaces this with the public collect intake page
 * (unlock gate, department picker, upload form). Kept trivial here so the
 * public /c/:token route resolves for tests/typecheck.
 */
export default function CollectPage() {
  return <div />;
}
