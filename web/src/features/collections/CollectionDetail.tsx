/*
 * Minimal stub — Task 5 replaces this with the full collection detail view
 * (roster, lifecycle actions, department management). Kept trivial here so
 * the /collections/:id route resolves for tests/typecheck.
 */
export default function CollectionDetail() {
  return <div />;
}
